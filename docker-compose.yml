# Traefik routes by domain; host port is for direct IP access (preview / before DNS).
#
#   Domain:  https://ahnoudtech.com
#   Direct:  http://<server-ip>:3002
#
# On the server: copy to APP_DIR (e.g. /root/ahnoud). Set GitHub secret APP_DIR to the same path.
services:
  app:
    image: ghcr.io/cavidyrm/ahnoudtech:latest
    container_name: ahnoudtech
    restart: unless-stopped
    ports:
      - "3000:80"
    networks:
      - traefik-public
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-public"
      - "traefik.http.services.ahnoudtech.loadbalancer.server.port=80"
      - "traefik.http.routers.ahnoudtech.rule=Host(`ahnoudtech.com`) || Host(`www.ahnoudtech.com`)"
      - "traefik.http.routers.ahnoudtech.entrypoints=websecure"
      - "traefik.http.routers.ahnoudtech.tls=true"
      - "traefik.http.routers.ahnoudtech.tls.certresolver=myresolver"
      - "traefik.http.routers.ahnoudtech-http.rule=Host(`ahnoudtech.com`) || Host(`www.ahnoudtech.com`)"
      - "traefik.http.routers.ahnoudtech-http.entrypoints=web"
      - "traefik.http.routers.ahnoudtech-http.middlewares=ahnoudtech-redirect-https"
      - "traefik.http.middlewares.ahnoudtech-redirect-https.redirectscheme.scheme=https"

networks:
  traefik-public:
    external: true
